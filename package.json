{
  "name": "expo-desktop-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "flatten:y": "node scripts/flatten-expo-desktop-protocols.mjs flatten",
    "flatten:n": "node scripts/flatten-expo-desktop-protocols.mjs restore",
    "format": "oxfmt",
    "changeset": "changeset",
    "release:version": "changeset version && pnpm install --lockfile-only",
    "release:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.47.0"
  },
  "lint-staged": {
    "*.{css,html,js,jsx,json,md,toml,ts,tsx}": [
      "oxfmt"
    ],
    "!(pnpm-lock).yaml": [
      "oxfmt"
    ]
  }
}
